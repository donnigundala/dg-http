server:
  host: "0.0.0.0"
  port: 8080
  
  middleware:
    cors:
      enabled: true
      allow_origins: ["*"]
    
    gzip:
      enabled: true
      level: 5
    
    request_id:
      enabled: true
    
    logger:
      enabled: true
    
    recovery:
      enabled: true
