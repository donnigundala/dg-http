package dghttp

const (
	// Binding is the standard key used to register the http transport provider in the container.
	Binding = "http"

	// RouterBinding is the standard key used to register the authoritative router in the container.
	RouterBinding = "http.router"

	Version = "1.1.0"
)
